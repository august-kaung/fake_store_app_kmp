LaunchedEffect                                -> act as init state if independent
                                              -> re-run when dependencies change
rememberCoroutineScope().launch               -> re-run with action
