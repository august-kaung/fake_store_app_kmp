CREATE TABLE order_items (
    id INTEGER NOT NULL,
    title TEXT NOT NULL,
    price REAL NOT NULL,
    qty INTEGER NOT NULL
);

insertOrder:
INSERT INTO order_items(id, title, price, qty)
VALUES (?, ?, ?, ?);
